//Write a C program to find maximum between three numbers using switch
#include <stdio.h>

int main(){
	
	int num = a; 
	int num = b;
	int num = c;
	
	printf("Enter 3 digits keeping a space: ");
	scanf("%d %d d%", &a, &b, &c);
	
	switch()
		case 1: if( a > b && a > c)
					printf("%d", a);
		
		case 2: if( b > a && b > c)
					printf("%d", b);
					
		case 3: if( c > a && c > b)
					printf("%d", c);
					
					
	return 0;
		
}
